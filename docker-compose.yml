version: "3.8"

services:
  servidor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monitoreo-servidor
    ports:
      - "5000:5000"
      - "6000:6000"
    volumes:
      - ./logs:/app/logs
      - db_data:/app/data
    restart: unless-stopped
    environment:
      - TZ=America/Bogota
      - IOT_DB_PATH=/app/data/database.db

  web:
    image: python:3.11-slim
    container_name: monitoreo-web
    working_dir: /app
    command: python3 web_server.py
    ports:
      - "8080:8080"
    volumes:
      - ./web:/app
      - db_data:/app/data
    depends_on:
      - servidor
    environment:
      - HTTP_PORT=8080
      - DB_PATH=/app/data/database.db
      - TZ=America/Bogota
    restart: unless-stopped

volumes:
  db_data:

networks:
  default:
    name: monitoreo-net